{
  "name": "claude-channel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.12",
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "build": "mkdir -p dist && bun build --compile src/index.ts --outfile dist/claude-channel",
    "build:linux-amd64": "mkdir -p dist && bun build --compile --target=bun-linux-x64 src/index.ts --outfile dist/claude-channel-linux-amd64"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "bun-types": "^1.3.12",
    "typescript": "^6.0.2"
  }
}
